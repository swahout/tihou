# 地方競馬予想プロジェクト — CLAUDE.md

## ⚠️ 絶対的な制約

**keiba.go.jp へのスクレイピングは礼儀正しく行うこと。**

- リクエスト間隔は最低 1.2 秒（`REQUEST_DELAY`）を厳守する
- 短時間に大量リクエストを送らない（特にキャッシュ構築時も `FETCH_DELAY=0.8` 以上を維持）
- ブロックされた場合は間隔を広げること。絶対にリトライループで突破しようとしない

---

## プロジェクト概要

NAR（地方競馬全国協会）主催の地方競馬を対象に、3着内確率を計算して上位7頭を選出するシステム。
データソースは `https://www.keiba.go.jp` のみ（PC版 netkeiba は JS レンダリングで取得不可）。

### 中央競馬（uma リポジトリ）との主な違い

| 項目 | 地方（tihou） | 中央（uma） |
|------|-------------|------------|
| データソース | keiba.go.jp | netkeiba.com |
| アーキテクチャ | ルールベース + 重み最適化 | 4モデルMLアンサンブル |
| 特徴量 | 着別成績 × オッズ × 枠番傾向 | 211特徴量（速度指数・血統等） |
| 最適化手法 | グリッドサーチ | Optuna (TPE) |
| 歴史データ保存 | キャッシュJSON（都度スクレイピング） | CSV（年別ディレクトリ） |
| 開催頻度 | 毎日複数場・複数レース | 週末中心 |
| 対応場数 | 12場 | G1レース個別対応 |
| 血統・調教データ | なし | あり |
| ペースシミュレーション | なし（将来検討） | あり |

---

## アーキテクチャ

### スコアリングロジック（predict.py）

```
top3_prob = normalize( sqrt(perf_score × mkt_score) × gate_bias )
```

#### perf_score（成績確率）

着別成績（通算・当場・当距離・当馬場）をベイズ平滑化した上で対数加重合成:

```python
log_score = (
    0.15 * log(r_overall)   # 通算成績
  + 0.30 * log(r_track)     # 当場成績
  + 0.30 * log(r_dist)      # 当距離成績
  + 0.25 * log(r_cond)      # 当馬場状態成績
) * weight_factor            # 馬体重変動ペナルティ
```

#### mkt_score（市場確率）

単勝オッズの逆数を全頭で正規化。

#### gate_bias（枠番バイアス）

当日の完了済みレース（3レース以上）から枠番別3着内率を集計。  
ベイズ平滑化後 [0.75, 1.35] にクリップ。  
**データ不足時（完了レース < 3）は gate_bias を適用しない。**

#### ベイズ平滑化

```python
def top3_rate(record, prior=0.33, alpha=5):
    total = sum(record)
    if total == 0:
        return prior
    raw = (w + s + t) / total
    return (raw * total + prior * alpha) / (total + alpha)
# alpha=5: 5戦で事前分布と実績が半々
```

### 重み最適化（optimize.py）

- 対象日のレース結果をキャッシュ（`cache.json`）して保存
- グリッドサーチ（step=0.05）で 市場/通算/当場/当距離 の4重みを探索
- 評価指標: **予測上位5頭中に実際の3着内馬が何頭入るか（top5_coverage）**

---

## ファイル構造

```
tihou/
├── CLAUDE.md          # このファイル
├── predict.py         # 当日予想スクリプト（全レース一括）
├── optimize.py        # 重みグリッドサーチ最適化
├── requirements.txt   # requests, beautifulsoup4, lxml, tabulate
└── cache.json         # 過去レースキャッシュ（gitignore済み）
```

---

## 対応競馬場

```python
VENUE_MAP = {
    "名古屋": 24,  "船橋": 19,  "大井": 20,  "川崎": 21,
    "浦和":   18,  "門別": 36,  "園田": 27,  "姫路": 28,
    "金沢":   22,  "笠松": 25,  "高知": 42,  "佐賀": 44,
}
```

※ BABA_CODE は keiba.go.jp の場コード。追加時は URL パラメータ `k_babaCode` の値を確認すること。

---

## よく使うコマンド

```bash
# 当日の名古屋競馬を予想
python predict.py

# 場所と日付を指定
python predict.py 2026/06/08 --venue 大井

# 過去の結果と照合して精度検証
python predict.py 2026/06/03 --venue 名古屋 --verify

# キャッシュ構築 → 重み最適化
python optimize.py
```

---

## データ取得の仕組み

### keiba.go.jp の主要エンドポイント

```
# レース一覧
/KeibaWeb/TodayRaceInfo/RaceList?k_raceDate=YYYY%2FMM%2FDD&k_babaCode=XX

# 単勝・複勝オッズ（馬名・騎手・体重変動も含む）
/KeibaWeb/TodayRaceInfo/OddsTanFuku?k_raceDate=...&k_babaCode=XX&k_raceNo=N

# 出馬表（着別成績テーブル付き）
/KeibaWeb/TodayRaceInfo/DebaTable?k_raceDate=...&k_babaCode=XX&k_raceNo=N

# レース結果（着順・馬番）
/KeibaWeb/TodayRaceInfo/RaceMarkTable?k_raceDate=...&k_babaCode=XX&k_raceNo=N
```

### 着別成績テーブル（`class='arrival'`）の構造

```
キー "全": 通算  (1着数, 2着数, 3着数, 着外数)
キー "場": 当場   同上
キー "距": 当距離 同上
```

※ 馬場状態別（良/稍重/重/不良）は `past_races` から手動集計。  
※ 着別成績テーブルは馬番順に並ぶが、出馬表のスクレイピング順と一致していることを前提としている。ずれが生じた場合は `arrival_tables[i]` のインデックスを確認すること。

---

## 現状スコアと課題

### 達成済み

- 当日全レース一括予想（時間・馬場状態・枠番傾向付き）
- ベイズ平滑化による少サンプル馬の安定評価
- 過去レースキャッシュ + グリッドサーチによる重み最適化
- `--verify` モードで精度の事後検証が可能

### 未解決の課題と改善候補

| 課題 | 現状 | 改善案 |
|------|------|-------|
| 騎手・調教師の評価 | 未使用 | 当場/当距離の騎手成績を特徴量化 |
| 脚質（ペース）考慮 | なし | 通過順データから逃げ/先行/差しを分類 |
| 上がり3F・タイム | なし | keiba.go.jp の出馬表から取得可能か確認 |
| 馬齢・性別補正 | なし | 若馬/牝馬のバイアスを検証 |
| MLモデル化 | ルールベースのみ | データが100レース以上蓄積したら LightGBM 導入を検討 |
| 複数場の同日開催 | 1場ずつ手動指定 | 複数場の並列予想 |

### 重み最適化の現状（2026-06-08時点）

`optimize.py` のグリッドサーチで探索した結果は `cache.json` に付随して保存。  
`TARGET_DATES` に新しい日付を追加してキャッシュを再構築すると精度が上がる。  
現在のデフォルト重み（predict.py のハードコード値）:
- 通算: 15%
- 当場: 30%
- 当距離: 30%
- 当馬場: 25%

**→ optimize.py を実行して最適重みが判明したら predict.py の `calc_top3_probs` の係数を更新すること。**

---

## 過去の試行と教訓

### データ取得

- **PC版 netkeiba (`db.netkeiba.com`) は使用不可**: JavaScript でレンダリングされるため `requests` + `BeautifulSoup` で内容が取得できない
- **keiba.go.jp の `table` タグは構造が変わることがある**: パース失敗時はまず `find('table')` の結果と HTML を直接確認すること
- **オッズテーブルの列順**: `[0]=枠, [1]=馬番, [2]=馬名, [3]=単勝, [4]=複勝下, [5]=複勝上, [6]=性齢, [7]=体重変動, [8]=負担重量, [9]=騎手, [10]=所属, [11]=調教師, [12]=変更情報`

### スコアリング

- **オッズゼロ馬への対処**: `max(odds_tan, 1.01)` でゼロ除算を防ぐ。オッズ未確定（発売前）の場合は予想しないか除外すること
- **gate_bias のクリップ**: [0.75, 1.35] の範囲に制限しないと1レースの偶然で大きく振れる
- **馬体重変動ペナルティ**: ±6kg 以内は影響なし。それ以上は 1% ずつ最大 15% まで減点。過去に「体重増≠悪影響」のケースもあるため現時点では控えめな設定

### 地方競馬固有の注意点

- **同一馬が異なる馬番で出走する**: 地方では同じ場に繰り返し出走する馬が多い。着別成績は累積なので過去成績が豊富でスコアが安定しやすい（中央と逆で良い面）
- **当場成績が最重要**: 地方馬は特定の場に専門化していることが多く、`r_track` の重みを高めに設定するのが合理的
- **移籍直後は当場成績がゼロ**: 新しい場に移籍した馬はベイズ平滑化でベースレートに引き戻される。ただし中央からの転入馬（元G1馬等）は要注意
- **騎手の差が大きい**: 地方競馬はスター騎手と若手の技量差が中央より大きいが、現在の特徴量には入っていない

---

## CLAUDE.md の更新ルール

**このファイルは生きたドキュメントとして常に最新状態を保つこと。**

| タイミング | 更新内容 |
|-----------|----------|
| 重み最適化の結果が出たとき | 「重み最適化の現状」セクションを更新 |
| 新しい特徴量を追加したとき | アーキテクチャセクションに追記 |
| スクレイピングの仕様変更を発見したとき | データ取得セクションに追記 |
| 精度検証でパターンを発見したとき | 「過去の試行と教訓」に追記 |
| 新しい競馬場に対応したとき | `VENUE_MAP` セクションを更新 |

更新の判断基準: 「次のセッションの Claude がこれを読んで同じ作業を再現できるか？」
